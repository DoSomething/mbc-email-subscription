mbc-email-subscription
==========================

A consumer to process email signups. Currently supports consuming the mailchimpSubscriptionQueue. Entries are signup in batches to the Mailchimp service.
